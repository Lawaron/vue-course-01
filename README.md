# vue-course-01

```bash
  docker compose up npm (--build)
  docker compose run --rm npm create vue@latest
  docker compose run --rm npm i
  docker compose run --rm npm i @popperjs/core bootstrap bootstrap-icons
```
